/** NestJS DI tokens for the Phase 1E retention-architecture slice — kept in one file, same pattern as ../audit/audit.constants.ts. */
export const RETENTION_POLICY_REPOSITORY = Symbol("RETENTION_POLICY_REPOSITORY");
export const RETENTION_HOLD_REPOSITORY = Symbol("RETENTION_HOLD_REPOSITORY");

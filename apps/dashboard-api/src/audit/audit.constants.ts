/** NestJS DI tokens for the Phase 1E audit-foundation slice — kept in one file, same pattern as ../auth/config/auth.constants.ts. */
export const AUDIT_EVENT_REPOSITORY = Symbol("AUDIT_EVENT_REPOSITORY");

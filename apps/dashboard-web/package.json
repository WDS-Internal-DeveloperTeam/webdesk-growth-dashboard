{
  "name": "@webdesk/dashboard-web",
  "version": "0.1.0",
  "private": true,
  "description": "Next.js App Router — presentation only, no database access, no business logic (ADR-0002). Phase 1A: root layout, placeholder shell, health page, error/not-found boundaries, env validation only — no final navigation or dashboard modules.",
  "scripts": {
    "build": "next build",
    "dev": "next dev",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint app lib components tests --max-warnings=0 && node scripts/check-css-tokens.mjs",
    "test": "vitest run",
    "test:integration": "playwright test"
  },
  "dependencies": {
    "@webdesk/configuration": "workspace:*",
    "@webdesk/shared-types": "workspace:*",
    "@webdesk/ui": "workspace:*",
    "next": "^16.3.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "@playwright/test": "^1.49.0",
    "@testing-library/jest-dom": "^6.6.0",
    "@testing-library/react": "^16.1.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.0",
    "jsdom": "^25.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.2.7"
  }
}

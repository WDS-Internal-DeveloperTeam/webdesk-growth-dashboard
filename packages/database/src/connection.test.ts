import { describe, expect, it } from "vitest";
import { getConnection } from "./connection.js";

describe("getConnection (Phase 1A placeholder)", () => {
  it("throws — no real implementation exists yet", () => {
    expect(() => getConnection()).toThrow(/Phase 1B/);
  });
});
